using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using NUnit.Framework;
using System.Reflection;
namespace TigerAppWPF
{
    [TestFixture]
    class TestRepartiteur
    {
        /*private static object RunStatic(Type classType, string method, object[] args)
        {
            var methodInfo = classType.GetMethod(method,
            BindingFlags.NonPublic | BindingFlags.Static);
            return methodInfo.Invoke(null, null,null,args,null);
        }

        private readonly Equity eqFr= new Equity("FR0000131906",100000,"FR","EUR","RENAULT SA",69.93,115476,"Renault SA",Rating.CalcRating("","BB+",""));
        private readonly Equity eqUs = new Equity("US0378331005", 100000, "US", "USD", "APPLE INC", 614.13, 101695, "Apple Inc", Rating.CalcRating("", "AA+", ""));

        [SetUp]
        public void Init()
        {}

        [Test]
        public void InEquityModule_equityFr()
        {
            bool reponse = (bool)RunStatic(typeof(Repartiteur),"inEquityModule");
            Assert.IsTrue(reponse,"Fail gros naze");
        }
        public void InEquityModule_equityUS()
        {
            bool reponse = inEquityModule(eqUs);
            Assert.IsTrue(reponse);
        }*/
    }
}
